class ItemModel {
  String itemCode;
  String prefix;
  String code;
  String size;
  String color;
  String pageNumber;

  ItemModel(this.itemCode, this.prefix, this.code, this.size, this.color,
      this.pageNumber);
}
